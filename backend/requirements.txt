# Django Core
Django==4.2.7
djangorestframework==3.14.0
djangorestframework-gis==1.0
django-cors-headers==4.3.1
django-environ==0.11.2

# Database
psycopg2-binary==2.9.9

# Task Queue & Caching
celery==5.3.4
redis==5.0.1

# Geospatial
geopandas==0.14.1
shapely==2.0.2
rasterio==1.3.9

# Data Processing
pandas==2.1.4
numpy==1.26.2
requests==2.31.0

# ML Framework - PyTorch with CUDA 12.1 support (RTX 3070 compatible)
# Install PyTorch with CUDA: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0  # PyTorch with CUDA support (install via index-url above)
torchvision>=0.16.0
torchaudio>=2.1.0

# JAX with CUDA 12 support for GraphCast (RTX 3070)
# NOTE: JAX with CUDA requires special installation - see installation instructions below
# Install manually with: pip install --upgrade "jax[cuda12_pip]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
# For now, keeping JAX optional - uncomment if you have CUDA 12 installed
# jax>=0.4.23
# jaxlib>=0.4.23

# Additional ML Dependencies
opt-einsum>=2.3.2
safetensors>=0.4.3

# Computer Vision
opencv-python>=4.8.0

# Optional: Prophet for time-series forecasting (pollution model)
# prophet>=1.1.5  # Uncomment if using Prophet for pollution forecasting

# Note: For CUDA 12.1 support on RTX 3070, install PyTorch and JAX separately:
# 
# 1. PyTorch with CUDA 12.1:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# 2. JAX with CUDA 12:
#    pip install --upgrade "jax[cuda12_pip]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
#
# 3. Verify GPU availability:
#    python test_gpu.py
